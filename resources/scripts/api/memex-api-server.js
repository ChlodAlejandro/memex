memex.dep.register("api.server", async () => {

    /**
     * Responsible for making calls to the Memex API's Server section.
     *
     * Handles all API calls to `/v1/server/*`
     *
     * @class MemexAPIServer
     */
    return class MemexAPIServer {

        // TODO version get static function

        // TODO load config static function

    }

});